{
  "service": "seerr",
  "description": "Seerr - Request Management",
  "compose_file": "seerr.yml",
  "setup": {
    "steps": [
      {
        "action": "wait_for_service",
        "description": "Waiting for Seerr to be ready",
        "params": {
          "service": "seerr",
          "port": 5055,
          "timeout": 120
        }
      },
      {
        "action": "log_info",
        "description": "Seerr access info",
        "params": {
          "message": "Seerr: http://${SERVER_IP}:5055"
        }
      },
      {
        "action": "log_info",
        "description": "Display API keys",
        "params": {
          "message": "API Keys for Seerr setup:\n   • Radarr: ${RADARR_API_KEY}\n   • Sonarr: ${SONARR_API_KEY}"
        }
      },
      {
        "action": "log_info",
        "description": "Setup guide reference",
        "params": {
          "message": "Follow setup guide: ${ROOT_DIR}/docker/POST-INSTALL.md"
        }
      }
    ]
  }
}
